#include <stdio.h>

/**
 *main - Write a program that prints its name, followed by a new line.
 *@argc: number of arguments
 *
 *return: zero
 */
int main(char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
